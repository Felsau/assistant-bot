# Render Blueprint — https://render.com/docs/blueprint-spec
# Deploy with one click via the "Deploy to Render" button in the README.
services:
  - type: web
    name: assistant-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
      - key: ANTHROPIC_MODEL
        value: claude-sonnet-4-6
      - key: TIMEZONE
        value: Asia/Bangkok
      # Auto-generated by Render — no need to set these.
      - key: TELEGRAM_WEBHOOK_SECRET
        generateValue: true
      - key: CRON_SECRET
        generateValue: true
      # Fill these in the Render dashboard when prompted (sync:false = secret).
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
      # Recommended: your Telegram user ID(s) so only you can use the bot.
      - key: ALLOWED_USER_IDS
        sync: false
      # Optional: set to enable voice transcription (leave blank to disable).
      - key: OPENAI_API_KEY
        sync: false
